/* Configures the local node by looking up the configuration
 * on Deepstream */

import * as logging from '../../lib/logging';

const log = logging.getLogger('NodeConfigurator');

const SERVICE_TYPE = 'node-configurator';
